#include <stdio.h>

int main() {
    int a[3];
    for (int i = 0 ; i < 3 ; i ++)
    {
        scanf("%d[%d]" , &a[i]);
    }
    for (int i = 0 ; i < 3 ; i ++)
    {
        printf("%d",a[i]);
    }
    
}
