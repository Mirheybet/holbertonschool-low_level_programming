#include <stdio.h>

int factorial (int ptr)
{
    if (ptr > 0 )
        return (ptr * (factorial(ptr - 1))) ;

}
int main() 
{
    int number= 5 ;
    printf("%d ",factorial(number)) ; 
    return 0 ;
}
